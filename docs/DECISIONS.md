# Decision Log

Running record of every significant decision, its reasoning, and what's still open. Append-only; newest decisions at the bottom. Reversals get a new entry pointing at the old one, never an edit.

## Decided

| # | Date | Decision | Why |
|---|------|----------|-----|
| D1 | 2026-07-11 | Content scope: book 1 + film 1, with film 2 contributing the Dueling Club / Expelliarmus | It's exactly what Violet knows (2/3 through book 1 read aloud; seen films 1–2) |
| D2 | 2026-07-11 | Genre: side-view point-and-click adventure-RPG; painted room dioramas + illustrated castle map for travel; no spatial navigation challenges anywhere | Kills the two things that wreck 6-year-olds in RPGs (getting lost, navigating under pressure); the format where painted art shines |
| D3 | 2026-07-11 | Progression = the spellbook; spells are verbs that gate world interactions; no stats, XP, or numbers | Real RPG ability-gating with zero reading or arithmetic overhead |
| D4 | 2026-07-11 | Chapters as the level system: stable engine, data-driven content modules (`src/game/content/chapters/`) | Incremental building; ship Ch. 1, grow chapter by chapter with her feedback |
| D5 | 2026-07-11 | Stack: Vite + vanilla ESM + full-window Canvas 2D + Vitest; headless World/Game split | House style proven 3×; deterministic chapter tests; Pixi/Phaser already tried and abandoned in the family |
| D6 | 2026-07-11 | Adopt robotgame hardening: asset manifest contract + `check:assets` gate, `assetUrl` SHA busting, `VersionWatcher`, Pages artifact deploy with branch-push fallback, five-layer deploy verification | Lessons already paid for once |
| D7 | 2026-07-11 | Hybrid art: AI-generated painted backgrounds (no people, no text in them) + code-drawn vector puppet characters/UI/effects | Rich rooms cheaply; exact control of Violet's look and animation; no spritesheet pipeline |
| D8 | 2026-07-11 | Voice-first dialogue: every line voiced (ElevenLabs, all-British cast), 1–3-word caption chips, tap-to-advance + replay; player choices are icon cards with no wrong answers | RPG dialogue without fluent reading; supports independent play |
| D9 | 2026-07-11 | Duels: telegraphed two-verb rhythm (Shield / spell openings), windows ≥0.7s, invisible rubber-band assist, phase checkpoints, comic losses with instant retry and enemy staying weakened | "Challenging at times, easy to get out of trouble" — tension without frustration |
| D10 | 2026-07-11 | Save: localStorage, versioned schema + migrations + auto-backup, autosave on flag mutation, export/import, new-game behind parent long-press | Her witch survives months of dev; wipe-proof |
| D11 | 2026-07-11 | Violet's design: light brown long *slightly messy* hair, brown eyes, purple as her signature color (lining, sneakers, pre-Sorting scarf) | Per dad; purple = her name |
| D12 | 2026-07-11 | The duel block is called "Shield" (not Protego) | One readable word; Protego isn't in her books/films yet; voice line still shouts it with flair |
| D13 | 2026-07-12 | **Stealth learning layer**: incantation assembly on first-learn only (letter/syllable tiles with phonics voice), potion labels + counted ingredients, name-on-envelope beat, Expelliarmus letter-tap finisher; governed by eight anti-ed-game rules (always diegetic, never gates the spine, never re-drills, failure = comedy, no school voice, dosage cap of one beat per scene, invisible adaptivity, the "tell test"); parent dial Off/Gentle/Stretchy | Dad wants subtle education without ed-game smell; Hogwarts makes learning diegetic — classes, incantations, and recipes are canon fiction |
| D14 | 2026-07-12 | Learning voice packs (letter names, phonics sounds, counting, syllable chants) recorded in the Charms Professor voice | Even phonics should sound like magic class, not flashcards |
| D15 | 2026-07-12 | **Siblings are a floor, not a ceiling**: keep their infrastructure DNA (manifest gates, deploy verification, headless testing) but go all-out creatively — living castle (tracking-eye portraits, floating candles, cinemagraph layers), seasonal year progression, ink-bloom transitions, spell-mastery visual growth, yearbook capture system | Per dad: "don't let them hold us back" |
| D16 | 2026-07-12 | Every magical moment must compose from the SET_PIECES.md technique toolbox ("if it moves, we drew it; if it's still, it's painted"); T3 flagships capped ~1/chapter, each with a pre-named fallback and a harness scene; new techniques require an M1 prototype spike before any chapter depends on them | The tight line: don't squash imagination, don't promise imagination we can't pull off |
| D17 | 2026-07-12 | Ch. 6 opens on Christmas morning; the invisibility cloak is a Christmas gift (the book's own placement); seasonal variants advance with chapters | Faithfulness + the year should feel lived |
| D18 | 2026-07-12 | Owl post from home between chapters doubles as the diegetic resume-recap; letter voice assets accept real family recordings as drop-in replacements | Recap without menus; family in the game |
| D19 | 2026-07-12 | **Device-reality rules** (red-team verified, full detail in ARCHITECTURE.md §Device reality): 5-canvas memory cap with `width=1` eviction + getContext-null canary (WebKit's 224–384MB global cap fails *silently transparent*); fixed-timestep accumulator sim (iOS rAF runs 30Hz in Low Power Mode, 120Hz on ProMotion) — supersedes soccer's variable dt; `ctx.filter` banned (disabled in Safari!) along with shadowBlur / per-frame gradients / frame-loop getImageData; ≤5 full-screen passes per frame, darkness overlay at half res; voice = mono AAC decoded per-chapter, music in `<audio>`; explicit FontFace.load gating; everything sRGB | The imagination has to survive contact with a real base iPad |
| D20 | 2026-07-12 | **Verification doctrine** (smoke-tested live, full detail in VERIFICATION.md): every visual is a pure function of (scene, seed, t) reachable via a harness URL; agents self-review keyframe PNGs against geometric illusion checklists, assemble GIFs for the human gate, and lock approved looks as per-environment goldens (pixelmatch 0.1 / >0.5% — never exact, never cross-platform); Playwright pinned exactly 1.58.2 (Chromium already cached on this machine); `.nvmrc` 22.17.0 because non-interactive shells here resolve node v14 | Agents can't watch 60fps motion — this is how they still prove it looks right |
| D21 | 2026-07-12 | Brick-wall class effects use nine-argument source crops from one decoded wall painting (3px gutters, ~1% overlap, untouched tiles left pixel-quiet, intact wall drawn beneath with per-cell reveal) and never allocate per-brick canvases; mirror-class effects mask an offscreen bounding-box canvas with a feathered-ellipse sprite via destination-in rather than raw `clip()` | The wall keeps its painted texture and kills hairline seams without violating the global five-canvas WebKit budget; the mirror path avoids cross-engine clip-AA divergence |
| D22 | 2026-07-12 | Hagrid’s guide voice is a gruff Scottish man, mastered to the project’s -16 LUFS dialogue standard | Pete’s live playtest feedback: the earlier smooth British take was too quiet and did not sound like the character he wanted |
| D23 | 2026-07-12 | Every character, including companion animals and owls, ships as richly illustrated animated vector puppetry rather than raster sprites or low-detail placeholder geometry; the hero owl is the minimum quality bar and owl motifs recur naturally through story objects and interface ornament | Pete and Violet care especially about the owls, and Pete explicitly rejected a cheap, low-poly, or placeholder-like final cast |
| D24 | 2026-07-12 | Readable story objects are player-paced: the opening animation settles into a persistent reading surface, and narration begins only from an explicit “Hear the letter” action | The original timer replaced Violet’s letter with a dialogue panel before there was time to read it |
| D25 | 2026-07-12 | Room changes use one temporary 1280×720 source-frame canvas held fully opaque until the destination painting is precomposited, then reveal it with an eased eight-point ink bloom for doors, sparkles for map travel, or a short crossfade for reduced motion; the scratch store is forced to 1×1 on completion, reset, failure, and destroy | Prevents first-visit fallback art from flashing through, delivers the authored magical transition, stays inside the five-full-screen-canvas WebKit budget, and releases scarce Safari canvas memory immediately |
| D26 | 2026-07-13 | **Visual Unification Pass** (full plan: docs/VISUAL_UNIFICATION.md) runs before any new content; the **Storybook Standard** (one fidelity tier — palette/line/form/light/texture/wobble/squint-test) becomes a mandatory illusion-checklist section; character rebuilds are reviewed side-by-side against generated painted reference sheets (`art/character-refs/`); Violet's playtest (the true M2 gate) happens after the pass | Pete's review + screen inspection: painted rooms are production-grade but puppets/UI/affordances sit at two lower fidelity tiers, reading as amateur; geometric checklists pass screens that fail taste |
| D27 | 2026-07-13 | One interaction-affordance language: warm gold shimmer + orbiting motes ON the object (two intensities: available / hint-escalated); dashed rings and floating diamonds removed; hidden collectibles get periodic glints + pet-sniff behavior; the map objective is always a glowing star | Dashed marching-ant rings read as debug hitboxes; the spec always called for one gold shimmer language |
| D28 | 2026-07-13 | On-screen text during play = caption chips (≤3 words) + proper nouns on title/chapter cards, nothing else; all instruction moves to voice + icons; parent panel exempt | Helper sentences ("Tap the page to continue", map subtitles, "Hold for grown-ups") are unusable by a non-reading 6-year-old and were masking weak visual language |
| D29 | 2026-07-13 | Pete's GIF review is a hard MERGE gate for visual work (PENDING.md entry + recorded approval before merge); goldens are blessed and `diff:goldens` wired into CI at the end of the unification pass | The build raced ahead of the human taste gate; eleven unprocessed review entries accumulated while "green" screens shipped |
| D30 | 2026-07-13 | The player-facing name is **Violet at Hogwarts**, aligned across the canvas masthead, browser title, installed-app title, and canvas accessibility label; the broom sight-word caption is **“Flying broom!”** while the voiced line remains “That broom looks fast!” | The new name states the fantasy immediately. “Flying broom!” is understandable without Harry Potter knowledge and stays within the three-word caption rule; “Nimbus 2000” can remain a future voiced detail rather than required reading |
| D31 | 2026-07-13 | **Affordance salience hierarchy + lifecycle** (VISUAL_UNIFICATION.md Workstream 2; supersedes D27's persistent-shimmer two-intensity model): three tiers — one **golden thread** (current objective, exactly one on screen, strongest shimmer; carried by the room exit when the target is elsewhere), **discoverables** (no persistent glow; staggered occasional glints + shimmer on press-down), **secrets** (rare faint glint + pet paws at the spot). Budget: one thread + ≤2 visible glints per 3s window, checklist-enforced. Spent hotspots drop out of advertisement mechanically (derived from quest/flag state); all affordances quiet during dialogue/set pieces/walking; salience tier exposed in `World.snapshot()` for headless budget tests | Pete's playtest: dashed rings appear everywhere at equal strength (five at once in Diagon Alley), don't distinguish next-step from optional, and persist after interactions are spent — a style bug, a missing attention hierarchy, and a missing lifecycle, fixed together |
| D32 | 2026-07-13 | **Velocity over ceremony — no human approval gates anywhere** (supersedes D29 and the golden-blessing half of D20/D26): the GIF-approval merge gate, `review/PENDING.md` queue, golden blessing ritual, committed-goldens requirement, and `diff:goldens` CI wiring are all retired; `diff.mjs`/`bless.mjs` remain as optional self-serve tools. Agents self-review captures strictly against the illusion checklists (they are the last reviewer), merge when CI is green (tests + content lint + asset checks — those stay), and deploy continuously. Taste-risky changes get a plain-language heads-up in reports, not a blocking request. Per-push five-layer deploy verification becomes a debugging tool. The chapter-before-next-chapter playtest rule softens to guidance. Pete's feedback arrives by *playing the deployed game* and lands here as decisions | This is a vibecoding project for one child, never seeing commercial release; queues waiting on dad were dragging velocity without adding quality the automated layers don't already provide |
| D33 | 2026-07-13 | **High-frequency commit + push** (convention lives in AGENTS.md): push to `main` after every self-contained green increment — several pushes per session, never one batch at the end; every push auto-deploys to GitHub Pages (~2 min), which is Pete's continuous test surface. Never push red; never end a session sitting on green work; stopping while red requires naming exactly what's broken in the report | Pete tests on the live Pages build; with D32 removing approval gates, the deployed game is the review surface and must always be current — and unpushed work on one machine is an unacceptable single copy |
| D34 | 2026-07-13 | The golden-capture environment identity advances to macOS 26.5.2 and now pins SHA-256 identities for both Andika weights and Almendra Bold *(renumbered from a duplicate D31 — concurrent sessions appended the same number; salience-hierarchy D31 keeps it, being referenced by number in tests and docs)* | The host update changed the raster environment, and a title-font change must invalidate captures when any bundled rendering font changes rather than hiding behind `fonts-none` |
| D35 | 2026-07-13 | Motion-review assembly is pinned to FFmpeg 8.1.2 across the executable guard, tests, verification doctrine, and build plan *(renumbered from a duplicate D32 — velocity-doctrine D32 keeps it, being referenced across five docs)* | Homebrew advanced the local review toolchain before the Visual Unification Pass; keeping the pin exact makes GIF/contact-sheet output reproducible instead of bypassing the environment gate |

| D36 | 2026-07-13 | **Text-rule exceptions** (refines D28): besides caption chips and title/chapter-card proper nouns, on-screen text is allowed for (a) **readable story objects** — the letter's parchment, spellbook incantations, rune tiles, potion labels: reading them IS the content per D13/D24, and their written words must match any narration verbatim; and (b) **short action labels on primary controls** (≤3 easy words from the caption vocabulary, e.g. "Hear the letter", "Again", the update prompt's "Reload"/"Later") | The blanket purge as written would have deleted the game's flagship literacy beat and its player-paced controls — helper *sentences* die, functional words stay |
| D37 | 2026-07-13 | **Violet's photo-matched look** (extends D11): she wears **dark-green rectangular glasses**, and her hair tone is a cooler **ash light-brown**; already reflected in the accepted reference sheet `art/character-refs/violet.png` and required of the VU-01 puppet rebuild | Pete supplied a real photo; the reference art followed it — the spec and ledger now follow the reference |
| D38 | 2026-07-13 | **O4 closed — hosting is public GitHub Pages** at peteallen.github.io/violets-wizard-game (public repo, sibling-consistent; free-plan Pages requires public), answered in practice during the M0 build | The deploy has been live since 2026-07-12; the open-questions table shouldn't list settled facts |
| D39 | 2026-07-13 | **Map content is authoritative, presentation is replaceable**: each validated map location owns one travel destination, objective-target mapping, stable vignette rectangle, and derived compatibility action; route geometry lives beside those locations. A pure `MapState` derives objective stars, soft-fog state, and travel intent from a world snapshot without mutating it or depending on the map painting | Replaces the hardcoded flowchart-map bypass with one testable content model, prevents authored destinations and click actions from drifting apart, and lets VU-05 change the illustration or renderer without changing navigation behavior |
| D40 | 2026-07-13 | **The opening letter is one continuous readable object** (refines D24): owl delivery, resting prop, and opening frame zero share one canonical code-drawn envelope, name, postmark, and irregular owl seal; unfolding settles into the same invitation used for reading; “Hear the letter” optionally narrates both clips without hiding the written words, while the separate “Let’s go!” action advances without audio | Pete’s live playtest exposed three visibly different envelopes, a giant face-covering opening prop, Pac-Man seal halves, and a mandatory narration path that replaced the letter with the contextless captions “HOGWARTS!” and “For Violet” |
| D41 | 2026-07-13 | **Violet earns her robes at Madam Malkin’s**: she wears a normal purple soccer jersey, dark leggings, and sneakers through the bedroom and early shopping trip; the robe picker is a live full-body dressing-room preview with twelve generous fabric-color choices, and only confirmation completes the purchase | Starting Chapter One already dressed as a witch spoiled the costume-shopping payoff; three abstract color cards did not show the visible, permanent choice they were making |
| D42 | 2026-07-13 | **Dialogue is one coherent storybook card**: one unrotated asymmetric deckled parchment owns the short caption; the active portrait attaches half outside the speaker-facing edge; separate, disjoint 88×88 replay and advance controls live on the opposite edge. Rolled scroll ends, nested caption panels, and floating controls are removed | Pete’s live screenshot showed five unrelated objects fighting for one strip of screen: misplaced scroll “bookends,” a detached portrait, a box inside a box, and replay/advance controls with no readable grouping |
| D43 | 2026-07-13 | **The Chapter Two placeholder has its own preview composition and a true-reset escape hatch**: a compact upper title plaque leaves the platform painting visible; Explore and Play again sit in one non-overlapping bottom action shelf; generic owl ornaments and choice cards are not used. A visible **Start fresh** action opens the existing confirmation and then clears all story/character/yearbook progress while preserving device settings; **Play again** remains the non-destructive replay sandbox | The generic chapter card plus generic choice renderer painted enormous buttons over its own title and produced stray owl drawings. Pete also needed a direct way to test from a genuinely fresh Chapter One save instead of replaying on top of completed state |
| D44 | 2026-07-13 | **Violet’s hair is warm light brown, not ash** (supersedes only D37’s hair-tone clause; its dark-green rectangular glasses remain): the shared puppet palette uses a warm light-brown base `#9b7654`, chestnut mid/shadow tones, and golden-brown highlights in every world, title, dialogue, and robe-preview rendering | The cooler photo-derived palette read visibly gray in the deployed game; the simple player-visible requirement is light brown hair |
| D45 | 2026-07-13 | **Hagrid leads by leaving first**: after his bedroom introduction he walks out through the bedroom door and disappears; after “This way!” in the Leaky Cauldron he walks out through the courtyard door and disappears. Violet stays behind with sparkle footprints and the golden-threaded door to follow; neither room ever asks for a second Hagrid tap | Pete’s live playtest exposed a broken conversational rhythm: “follow me” left Hagrid standing in place as another target, so following him felt like repeating the conversation instead of pursuing someone already on the move |
| D46 | 2026-07-13 | **Room integration is authored, not baked**: each room may declare a left- or right-side key light, defaulting left; the bedroom declares its visible right-window key, and runtime puppets and dialogue portraits mirror their shadow planes and rim to match. Every full-body character and persistent diegetic HUD object gets a soft contact shadow. Bedroom Hagrid uses visible articulated arms, broad boots, a shaped multi-plane coat, and separate shaded hair, moustache, and beard; casual Violet uses joined shoulder/elbow/wrist anatomy, childlike long-body proportions, a high-contrast three-tone blue-violet soccer jersey, dark leggings, slate trainers with a purple accent, light-brown hair, and dark-green dimensional glasses. Interaction gold stays outside character silhouettes as a contour and motes | The bedroom painting already had convincing directional sunlight and cast shadows, but the overlaid cast floated, carried a contradictory generic rim, and collapsed into flat shapes; Hagrid also had gold stains painted across him, while Violet’s detached stick arms and flat room-purple outfit read as placeholders |
| D47 | 2026-07-13 | **The room owns composition and progression** (refines D40–D42 and D46): Hagrid remains the broadest/tallest friendly adult but fits beneath any doorway he visibly uses rather than obeying a literal two-times-height reference note; puppet and companion shadows are planted in floor space independently of bob, hop, tilt, and sway; every owl/pet/set-piece receives the room key light; letter and dialogue compositions avoid every live figure and the current objective scenery; the satchel and wand HUD objects do not appear or capture input until Violet earns them | The first integration pass added detail but still left a too-tall Hagrid, shadows riding upward with bodies, a room-blind owl, letter/buttons over Violet, dialogue portraits over non-speakers, and empty/unowned HUD props in the opening bedroom |
| D48 | 2026-07-13 | **SP-E spike PASSED — characters move to AI-painted sprite parts** (supersedes D7's code-drawn-character half; D23's illustration-quality bar unchanged, the medium changes): each character is generated as a paper-doll part sheet in the room style, sliced by `art/spikes/sp-e-violet/slice.mjs` (modal-border flood key, dominant-component isolation, feather, trim), and assembled on the existing puppet animation grammar (bob, sway, hip-pivot walk, blink via head-part swap). Proof: harness scene `character-sprite-spike-review` — the painted Violet reads as the reference sheet's sibling next to the code-drawn puppet, from one $0.03 generation. Rollout: this becomes VU-01/02's approach — Violet first (jersey + robed sheets), then Hagrid (from his existing reference), shopkeepers, pets; the bezier character functions are deleted per character as each converts; reference sheets graduate from "style targets" to part-sheet sources | Pete: "all of the characters look like shit… is there a better approach?" — the image model is a better character artist than hand-authored beziers will ever be; libraries like Spine/Rive solve animation for human artists in GUI editors, not art for agent pipelines |
| D49 | 2026-07-13 | **Retired by D64**: the separate visual-review-agent layer is no longer part of the project workflow | Pete removed the extra gate; deterministic capture inspection, the full build, and deployed play remain |
| D50 | 2026-07-13 | **Always-hot conversion pipeline with in-chat progress images** (extends D48): the character rollout runs as a continuously iterating queue in roster order — Violet regen, Hagrid, Wandmaker, Madam Malkin, Menagerie keeper, then the hero owl and pets once a creature rig exists; generated sheets are inspected as they land, visible defects become explicit constraints in the next prompt, and every produced image is posted in chat so Pete watches progress live | Pete: "We need a workflow so we're continually iterating. If there's a break, bring in a new character. Show me progress images periodically in the chat" |
| D51 | 2026-07-13 | **Painted rigs ship live immediately and iterate in place** (extends D48–D50; per Pete): the best-available sliced part set for each character renders in real gameplay now — casual Violet (SP-E v1 parts) and Hagrid (SP-F v4 parts) through `SpriteRig` intercepts in `CharacterRenderer`, with automatic bezier fallback wherever the rigs can't yet express the moment (dialogue portraits, the wand hand, robed Violet, still-decoding images) and `medium: 'bezier'` pinning review-scene comparisons. During dial-in, source inspection drives part swaps and assembled capture inspection drives rig tuning. Harness scene `hagrid-sprite-review` holds the assembled Hagrid beside the code-drawn one | Pete: "start implementing this into the actual game and pushing to GH. We can keep swapping out sprites as we dial things in" — the deployed game is the feedback surface |
| D52 | 2026-07-13 | **Reference sheets pose characters upright-neutral**: personality posture — the Wandmaker's scholarly stoop, any lean or slouch — is a rig/pose-level trait, never painted into reference sheets or part sheets | The Wandmaker v2 ruling: upright-neutral is the slice- and rig-friendly canon, and a stoop baked into the identity anchor would propagate into every future generated part |
| D53 | 2026-07-14 | **Two-tier reference acceptance**: when repeated regenerations across model families keep failing a reference only on craft axes the part-sheet generator itself cannot exceed (canvas tooth, full-body shadow cooling, small-scale hand articulation), the best identity-locked sheet may be a **provisional identity anchor** while craft is judged directly against the Storybook Standard and room paintings; the provisional flag and outstanding findings stay in the prompt record. First application: Wandmaker v3 | Repeated rolls produced complementary but never complete sheets, so a hard perfection gate blocked usable identity work |
| D54 | 2026-07-14 | **The cast restarts from one inspected Gemini lineage and aligned-canvas variants** (supersedes D48's bounding-box paper-doll assembly, D51's live swapping, and D53's multi-model exception for character art): every canonical character and every production edit uses `google/gemini-3.1-flash-image`; an assembled neutral source is inspected before derivatives; each edit changes one localized region whose unchanged pixels come from the approved canonical source; all runtime layers share one canvas and ground anchor, carry explicit sockets/bounds, include left/right room-light variants, preload before play, and fail validation instead of silently changing character, pose, or medium. Shipping character assets live in `public/assets/art/characters`; source art and provenance remain under `art/characters`. Each character gets clean full-clip, expression/portrait, reduced-motion, and in-world harness scenes at both required resolutions before replacing the current live puppet | A single model plus preserved-pixel local edits controls identity; aligned canvases remove guessed joins; explicit clips, sockets, preload, and assembled capture inspection make the whole-cast animation tractable without sacrificing save compatibility |
| D55 | 2026-07-14 | **Violet V8 is the locked canonical Violet by explicit owner art direction**: the exact RGB pixels Pete selected are stored losslessly as `art/characters/violet/canonical/casual-approved.png`; automated concerns about the neutral figure's measured head ratio, limb bulk, shoe angle, and lack of a baked shadow are decided away and must not trigger another neutral regeneration. All expressions, poses, robes, and left/right lighting variants use `google/gemini-3.1-flash-image` from this exact source. The runtime owns the floor shadow, and assembled puppets remain subject to ordinary capture inspection | Pete, after selecting the displayed V8 image: “This one is perfect, why are we still iterating?” |
| D56 | 2026-07-14 | **Violet's approved facial states are inspected at portrait scale while the matching full-body row proves extraction, registration, and grounding**: neutral, blink, talk A, talk B, wonder, proud, and curious remain accepted; their clean scene shows each as both a readable portrait and an exactly aligned grounded figure, but similarity between wonder and curious at reduced full-body scale does not trigger another face regeneration | The pending pose vocabulary is the intended channel for making those emotions unmistakable in world context |
| D57 | 2026-07-14 | **Character production is batch-first and game-first** (retains D54's single-model/no-fallback rule, aligned canvases, explicit runtime mapping, and source/shipping separation, D55's locked Violet V8 identity, and D56's accepted facial states): inventory the states the current game requests, generate one coherent multi-view and core-action sheet per character with `google/gemini-3.1-flash-image`, inspect the proposed shipping subset before deterministic slicing, integrate the complete character immediately, then capture its harness and in-world animation at both required sizes for assembled inspection. Only player-visible defects prompt same-model refinements. Each complete green character is pushed before work advances | Batch inspection and immediate play preserve the quality checks that matter while making deployed, testable characters the measure of progress |
| D58 | 2026-07-14 | **A production batch is a source library, not an all-or-nothing perfect grid**: source inspection names the panels proposed for shipping and focuses on defects in those figures, not unused figures or equal-cell topology. Deterministic connected-component isolation, common-canvas placement, and edge cleanup may correct sheet layout without repainting. Violet's selected action figures are normalized to world scale, matte artifacts are repaired from opaque art donors, the near-duplicate casual pass frame is omitted, and no clip cuts between incompatible wand designs | Normal gameplay exposes whether remaining variation matters to a player without forcing another hidden-art loop |
| D59 | 2026-07-14 | **Violet has no authored speech or voice clips** (supersedes D30's voiced-line clause): her reactions and observations remain readable through animation, expression, objective copy, and environmental response, but the game never speaks on her behalf | Pete said Violet should not have speech or voice anywhere; keeping her silent leaves the player in control of her character instead of assigning her an authored voice |
| D60 | 2026-07-14 | **The illustrated map is optional for Diagon Alley travel**: every street-facing shop door is a direct world interaction that walks Violet to the threshold and enters that building; the map remains a convenience, not a required intermediate screen | Requiring the map after Violet was already standing outside a visible shop made the street feel like a menu backdrop rather than a place she could explore |
| D61 | 2026-07-14 | **Building exits are doorway traversals, not room swaps**: choosing an exit queues Violet's walk to and through the interior door before the transition begins, and the destination places her outside the matching street entrance | Appearing outside immediately after tapping an exit broke physical continuity and made the character animation feel disconnected from the painted world |
| D62 | 2026-07-14 | **The brick-wall opening owns one continuous destination reveal** (supersedes D21's intact-wall-under-each-cell reveal detail): the full-scale Diagon Alley scene is already behind individual opaque moving bricks, remains at one stable scale through the opening, and becomes the active room with no second transition wipe | Seeing the old courtyard behind exploding bricks followed by a separate transition contradicted the magic of opening a passage; the new street must be the space revealed by the bricks themselves |
| D63 | 2026-07-14 | **Production character loading is clip-scoped and cache-bounded** (supersedes D54's blanket preload-before-play clause): normal gameplay requests only the active appearance and clip plus its idle/blink baseline, limits each rig to two concurrent image decodes, retains the last displayed frame while a replacement loads, and evicts least-recently-used decoded frames beyond a bounded cache. Exhaustive whole-character preload remains only for deterministic review captures | Full-frame Violet and Hagrid could otherwise start roughly 27 MiB of transfer and about 140 MiB of decoded image memory before play, with future outfits, actions, shopkeepers, pets, and chapters making that startup burst grow without bound |
| D64 | 2026-07-14 | **There is no separate visual-review agent or approval gate** (supersedes D49 and the independent-review clauses in D50–D58): authors inspect source art and the assembled game at the required capture sizes against the Storybook Standard, fix visible defects, run the full local build gate, and ship green increments. The separate review brief is retired; another agent may still be asked for an opinion when useful, but it is never mandatory and never blocks slicing, integration, commit, or push | Pete explicitly removed the mandatory reviewer after it created queues, misleading limit messages, and repeated process pauses; deployed play remains the feedback surface |
| D65 | 2026-07-14 | **Characters, chapters, rooms, and scenes scale through explicit package contracts**: a character has one canonical `character.*` identity, immutable definition, and lazy world/portrait runtime; a chapter descriptor lazily loads pure composed content separately from presentation and harness registrations; rooms own physical space while explicitly ordered scenes own story layers. Duplicate registrations and unresolved references fail before play. A counted architecture baseline blocks any new concrete routing or source-art import and must ratchet to empty as the existing monoliths move. Legacy save and harness meaning crosses the boundary through deliberate adapters and aliases, never by weakening the new contracts | A dozen chapters and hundreds of characters cannot remain safe if identity, rendering, presentation, assets, loading, and story routing are inferred from switches, substrings, import side effects, or insertion order; making dependencies explicit lets unrelated content stay unloaded while preserving Violet's current adventure |
| D66 | 2026-07-15 | **Actor roles and character identities travel together during renderer migration**: every chapter NPC record keeps its legacy `npc.*` role ID and metadata while declaring one required canonical `character.*` identity. Headless world snapshots expose depth-sortable actor records containing both IDs and their render state, and dialogue exposes the speaker's canonical portrait identity. Room-authored display animals use the same actor path as the playable cast | Existing saves, harness fixtures, quest actions, and dialogue speaker references must keep their current meaning, while new character packages need exact identity-based dispatch with no role-name switches or guessed fallbacks |
| D67 | 2026-07-15 | **Chapter packages and standalone maps version independently during the v2 cutover**: chapter validation dispatches exact version-1 and version-2 roots; a v2 chapter requires the composer’s complete package collections and a scene-order bijection, each v2 scene requires explicit `order`, room, spawn, condition, and entry actions with only declared story-layer fields, package registrations are category-owned and globally unique, and nested action arrays retain exact payload validation. Standalone maps and the Chapter Two placeholder remain explicitly v1 until migrated; stable `npc.*`, set-piece, map, route, save, and harness identities cross through deliberate adapters rather than renames | One shared version flip would reject the live Chapter One projection, Chapter Two, and the standalone Diagon map at once, while accepting unordered scenes, duplicate registrations, or loosely checked action payloads would preserve the ambiguity D65 is removing |
| D68 | 2026-07-16 | **Violet is sorted into Gryffindor**: the Sorting Hat may ask two or three icon questions about what she cares about and use her answers to personalize what it says, but the canonical outcome is Gryffindor. Her saved house, scarf, Great Hall banners, common-room treatment, and later house continuity all follow that result; other house variants are deferred until deliberately needed | Pete: “Let's lock it on Gryffindor.” A fixed outcome makes the Chapter Two script, art, persistence, and first common room concrete without building four speculative branches |
| D69 | 2026-07-16 | **Familiar characters and recognizable story landmarks orbit Violet's original adventure**: Harry, Ron, Hermione, and other established characters may appear from time to time as classmates and allies, and the story follows the original landmarks closely enough that Violet always understands the situation, but scenes and dialogue do not strictly recreate the source sequence. Violet remains the protagonist and owns the adventure's decisive actions | Pete confirmed that the original characters should recur without displacing Violet or turning the game into a scene-by-scene reimplementation |
| D70 | 2026-07-16 | **Chapter Two asset production is first-pass, game-first**: the earlier pause on audio and raster generation is lifted. Generate one solid candidate for each immediate room, character, voice, music, or sound need; integrate it; fix obvious defects visible in the assembled game; then deploy it for Pete to test. Do not hold the chapter behind speculative polish or repeated generation loops | Pete: “Get them in, get them pretty good, let me test and I'll give feedback.” Deployed play is the refinement loop |
| D71 | 2026-07-16 | **A permanent Start fresh action lives on every satchel page**: it opens the existing explicit confirmation before clearing story, character, collection, and yearbook progress; cancelling returns to the same satchel page, while accepting preserves Violet’s name and the device’s sound, movement, and learning settings | A reset hidden behind the grown-up keyhole or limited to the completed-chapter preview is not reliably available during testing or ordinary play; a visible satchel action keeps recovery reachable without weakening the confirmation safeguard |
| D72 | 2026-07-16 | **The satchel is a named travel journal, not a control dashboard**: Violet’s ownership mark and the Map/Cards bookmarks stay grouped on the left page; the labeled grown-up keyhole and permanent Start fresh action form a quieter right-page utility cluster; close is the only X. Every map storefront has its proper name, Violet’s current place uses a plum Here marker, and the current objective keeps the one gold star even when no salience animation is active. The card review always shows the full four-card production spread | The old header gave five unrelated actions equal weight, split the title across the gutter, reused X for both reset and close, and connected four anonymous destination badges; a player could not tell what the places were, where Violet was, or which X was safe |
| D73 | 2026-07-16 | **Satchel controls are painted props with explicit task hierarchy** (refines D72): generated stitched-leather Map and Cards bookmarks are the only top navigation; generated grown-up and Start fresh props sit apart on the bottom book edge; the generated close seal is the sole top-right exit. The map says Choose a place, uses Here for Violet’s current street, gold checks for finished stops, and a written Next flag plus star for the required destination. Generated source art and prompts stay under `art/ui/satchel`, while keyed WebP assets ship under `public/assets/art/ui/satchel` | A context-free reviewer of the earlier D72 layout still chose Ollivanders instead of the intended Menagerie, read Cards as possibly disabled, and mistook corner flourishes for controls. A fresh reviewer of this layout correctly described the screen, finished stops, current place, and Menagerie as the exact next tap without repository context |
| D74 | 2026-07-16 | **Chapter replay protects story progress while preserving discovered keepsakes**: each replay runs in a temporary chapter save initialized from Violet’s durable settings, existing cards and treasures, and the character state needed to enter that chapter. Replay-only story flags, choices, house, points, chapter completion, resume position, and yearbook moments never overwrite the canonical adventure. On replay exit, only newly discovered cards and treasures merge into the canonical collection; already collected keepsakes remain present so their one-shot pickups stay retired | Replaying should never roll Violet’s real adventure backward or create duplicate collectible prompts, but finding a missed card or treasure should still count permanently |
| D75 | 2026-07-16 | **Satchel place art is textless and chapter navigation is truthful** (refines D72–D73): Diagon Alley, Ollivanders, Madam Malkin’s, and Menagerie each use a generated textless painted destination panel arranged as one left-to-right journey; exact names and Here/complete/Next state remain code-rendered. Every painted control and place declares a blank label rectangle whose text is centered on both axes. A chapter with no authored map opens to one centered Cards bookmark and exposes no Map target. Registered review scenes cover early and late Chapter One maps, Chapter One cards, Chapter Two cards-only, and Chapter Three cards-only at both required capture sizes | The code-sketched shop badges were visibly weaker than the painted controls, baseline-positioned labels drifted inside asymmetric art, and the old cards review falsely implied that Chapters Two and Three still had the Chapter One travel map |
| D76 | 2026-07-16 | **Chapter completion is one durable transaction, and save schema v3 makes the resume point explicit**: `chapter.complete` is terminal, requires its owning chapter and a registered next chapter, builds one candidate containing completion markers plus the destination chapter’s exact start, flushes that candidate once, and only then adopts it in the shared runtime. A failed flush leaves the previous save and final narrated page available for retry. Schema v3 preserves every v2 field and the existing storage keys while extending resume with `dialogue: null | { script, node }`; exact v1 and v2 validators remain supported, and the v2→v3 migration repairs completed Chapter Two saves stranded on its retired end card by moving them to Chapter Three’s registered start while retaining the raw v2 backup | The earlier two-write handoff could durably mark Chapter Two complete while leaving Violet in a now-ineligible card room, then lose the transient event needed to escape it. Atomic adoption prevents partial completion, and the versioned dialogue cursor creates a safe foundation for resuming only visible authored lines or choices without re-running hidden actions or rewards |
| D77 | 2026-07-16 | **Durable progress may repair a stale same-chapter resume point without changing story progress**: content-owned checkpoint redirects run after schema migration, back up the original stored save, and rewrite only the resume cursor. Chapter One’s ticket receipt now carries any incomplete save stranded in Diagon Alley to its chapter card, where the visible Continue button can skip the decorative page-turn wait immediately | A real save could durably contain `ch1.ticketReceived` while still pointing at Diagon Alley; reloading removed the completed quest and Hagrid interaction, while the chapter-card button looked enabled during an input-blocking four-second set piece |
| D78 | 2026-07-16 | **Each resume recap plays automatically once per story milestone**: presenting a recap records its chapter-owned ID in the save’s story receipts. Repeated resumes at the same milestone enter quietly with the current dialogue or objective, while reaching a later recap milestone makes that new recap eligible | A save parked at the Sorting Hat replayed the older train recap on every reload because recap selection used quest progress but never remembered presentation; retaining milestone orientation without looping stale narration keeps frequent stops and live-update reloads pleasant |

| D79 | 2026-07-16 | **Chapter 3's educational minigames are magical performances with immediate invisible assistance**: Lumos uses recessed letter-shape matching with continuous wand-brightening rewards; Wingardium Leviosa uses an unfailable syllable chant whose feather rises throughout; compact learning interactions use a 3/6/9-second hint ladder, and one wrong attempt immediately clarifies the route. The first tile teaches itself, mastered spells are never re-drilled, and the parent dial changes assistance without changing story or rewards | Violet will detect and reject an educational wrapper, and she is likely to ragequit when the intended action is not immediately legible. Hogwarts classes make literacy diegetic; fast convergence and continuous spectacle let her experience competence without turning the game into a quiz |
| D80 | 2026-07-16 | **A version-2 chapter must be internally complete and own every durable effect it authors**: full dialogue and quest contracts, dialogue reachability, chapter-owned flag, choice, and reward writes, declared yearbook moments, completion ownership, registered destination chapters, and registered card references are validated before play. The linker obtains these references through the action registry and resolves destination chapters through the lazy chapter catalog, so placeholders can link without loading their content | Shape-only package validation allowed malformed graphs, foreign writes, undeclared keepsakes, and broken handoffs to survive until a player reached them. Making every durable reference explicit keeps a dozen chapters independently loadable without weakening save ownership or production startup |
| D81 | 2026-07-16 | **Painted materials replace code-shaped placeholder surfaces without baking in behavior** (refines D40 and D72–D75): the returning title action is one clearly folded owl-post envelope with a pressed owl seal and live code-rendered action text; the satchel is one painted blackberry-leather travel folio whose pages sit behind live tabs, destinations, routes, markers, and utilities; earned cards use a painted transparent-window frame around the live portrait and name, while unearned cards use a welcoming closed pocket rather than a lock. Chapter Two completion, Sorting, welcome, and character-objective overlays stay small, physical, and attached to the authored room instead of covering it with flat plaques or enclosing people in abstract rings | Pete could not tell what the title object was and called out the satchel as another mismatched surface. The room paintings, destination art, and portraits were already strong; repainting the material holders preserves all save, input, text, and navigation contracts while bringing the assembled screen up to the production art around it |
| D82 | 2026-07-17 | **Violet's Field Kit gives every interface material, object, and state one stable meaning** (refines D40, D72–D75, and D81): blackberry leather holds containers and sibling navigation; parchment carries readable story, labels, and choices; aged brass is mechanism, boundary, and grown-up utility; wax closes or confirms; woven cloth carries ceremony and house identity; muted burgundy marks destructive utility; enchanted gold belongs only to the current objective or instant of activation. Every reusable component separates its painted physical shell from live text, portraits, routes, and save-dependent state; owns one action with an at-least 88×88 target; uses the applicable named rest, pressed, selected, unavailable, focus, busy, success, error, and destructive states; and protects the room as the story stage. Accessibility focus may use cleaner high-contrast geometry than the illustration, and every reachable surface-state family declares deterministic review fixtures at both required sizes | The painted satchel established a quality bar but not yet a reusable grammar. Without stable material semantics, named interaction states, a live-content boundary, and inverse harness coverage, later panels could be individually attractive while still feeling inconsistent, hiding behavior in baked art, competing with the room, or omitting reachable states from review |
| D83 | 2026-07-17 | **Visible story progress resumes from durable, idempotent runtime receipts**: schema-v3 dialogue cursors store only visible lines or choices and obey each script's restart policy; non-replayable dialogue waits for any launched set piece and continuation before its cursor and receipt commit together; version-two quest entry, completion, rewards, and their receipt commit as one candidate; and a set-piece receipt always names the requested logical performance across normal, reduced-motion, fallback, skipped, and reconstructed paths. Historical version-two saves silently adopt the quest prefix they already reached, a final dialogue receipt and final quest effects join chapter completion's single candidate, and version-one chapters retain their earlier condition-derived quest behavior without manufacturing incompatible receipt IDs | Reloading or losing a write between a hidden action, visual performance, reward, and scene transition could otherwise repeat dialogue, duplicate effects, open a stale blocking scene, or mark a chapter complete without all of its consumed story state. One receipt boundary per logical outcome makes retries safe without changing older saves, reinterpreting the version-one contract, or tying simulation truth to presentation timing |

| D84 | 2026-07-17 | **Learning a spell and casting a mastered spell are separate deterministic states**: a package-authored one-time learning beat owns ordered units, assistance, continuous class feedback, completion, and the durable `spell.learn` action; the shared spellbook controller separately owns closed, fan, targeting, and casting state, admits only known stable spell IDs, exposes all matching targets, and runs a spell target's authored actions only after a valid cast. Lumos and Leviosa are permanently identified as `lumos` and `leviosa`, and cast counts and mastery continue in the existing save spellbook without tying simulation truth to Canvas, audio, the DOM, wall time, or unseeded randomness | The learning ceremony should feel special once without re-drilling mastered magic, while every later cast must remain a reusable world verb whose target authorization, save mutation, replay behavior, and headless tests cannot be bypassed by an ordinary tap |
| D85 | 2026-07-17 | **Chapter Three owns its castle route and keeps optional promises visibly asleep**: one chapter map owns the autumn common room, Charms classroom, and three night corridors with package-authored textless destination art plus unlock and completion conditions; labels, routes, fog, Here/Next state, and the gold objective remain live. The quest journal keeps one active main thread and shows accepted but currently impossible side quests with a sleeping silver star; Fix the book has no map star, cast target, hint trail, or gold advertisement until Reparo is known. The ghost remains an unnamed original Friendly Ghost, and listening now grants the Bertie Bott card | Chapter One's Diagon map cannot truthfully describe castle travel, and an unresolved future-spell promise must remain rewarding and memorable without competing with Trevor's current route or falsely suggesting that Violet can repair the torn book now |
| D86 | 2026-07-17 | **Chapter Three's Off mode preserves the magical performance, and Violet remains silent** (refines D59 and D79): Off removes distractors but still advances Lumos through five guided rune taps and Leviosa through all six chant beats; Gentle and Stretchy may add the documented choices, but every mode reaches identical spells, rewards, flags, and celebration. Violet has no generated or authored speech: the player, live spell lettering, her animation, Flitwick's package-authored coaching, and effects carry Lumos, Leviosa, and Found Trevor | Skipping the ceremony in Off would erase the chapter's story payoff rather than merely remove educational pressure, while giving Violet isolated spell/exclamation lines would break the established silent-protagonist boundary and create a voice identity the rest of the game deliberately does not use |
| D87 | 2026-07-19 | **Readiness belongs to the next complete composition, with visible recovery**: a lightweight storybook boot surface appears before the game modules and offers a generation-safe Retry if startup fails. The title reveals only after its backdrop, action, and currently visible Violet pose are ready; adventure entry, room transitions, and set pieces keep the last complete title or room frame while their required presentation, painting, and visible character frames prepare. Incoming set-piece time, music, and voice remain deferred, and a required-load failure leaves the complete frame in place with retry instead of revealing fallback art. Registered boot and in-game loading/failure scenes prove both sizes and reduced-motion meaning | On a throttled connection, a blank page followed by clickable vector art, partially replaced characters, and narration ahead of the room made the game feel broken for more than a minute. Treating a coherent frame as the reveal boundary makes network delay understandable without pretending that every asset in the book must load first |
| D88 | 2026-07-19 | **Chapter content uses active-plus-next injection, while presentation activates on demand** (completes the loading intent in D65): descriptors stay lightweight at startup; before `World` construction the runtime dynamically loads the resumed chapter and its immediate successor into an injected synchronous registry containing their content, maps, recaps, and asset keys. Presentation modules activate for the chapter being prepared, and replay or a later transition expands the scope before switching. Generic simulation remains synchronous and later chapters do not enter the title-critical module graph | Importing every chapter to preserve `World`'s synchronous API made startup transfer grow with the whole book. An explicit small runtime scope preserves deterministic code and safe handoffs without asking a slow network to download scenes Violet cannot yet reach |
| D89 | 2026-07-19 | **Character PNGs are production sources; lossless WebPs are the shipping derivatives**: generated sheets, accepted source images, prompts, hashes, and provenance remain unchanged under `art/characters`; deterministic extraction writes same-frame lossless WebP assets under `public/assets/art/characters`, and runtime definitions reference only those derivatives. Validation compares dimensions, alpha, visible RGB, and composites over contrasting backgrounds, allowing differences only in RGB hidden beneath fully transparent pixels | The 78 shipped PNG frames transferred about 49.1 MB before chapter growth; the visually identical WebPs transfer about 24.8 MB, a 49.5 percent reduction, without reopening approved art or weakening source provenance |
| D90 | 2026-07-19 | **Asset delivery is SHA-revisioned and cached by bounded demand after title readiness**: production asset URLs carry the full build SHA, and each emitted service worker owns one cache named for that revision. It precaches the shell, fonts, title painting and action, and critical title frames; only visited images, scripts, styles, and fonts join at runtime. Range requests and `version.json` bypass the worker, registration waits for a ready title and an idle window, and old revision caches are removed only after the new worker activates | Repeat visits should reuse the book Violet has already opened, but a first visit must not compete with an 86 MB precache or inherit stale Pages assets. Revision isolation, narrow precache scope, and demand caching improve return loads without changing streamed audio or update detection |
| D91 | 2026-07-19 | **Production and review entry points are isolated by construction**: harness modules build into an explicit review chunk, `harness.html` alone marks itself for auto-boot, and production generation fails if any character runtime statically reaches harness or boot-review code. The dependency-free loading surface renders through system-font fallbacks immediately while Canvas still waits for exact authored fonts | A real production-preview run found Rollup co-locating review code with character runtimes. Importing Violet first deadlocked title boot on the harness's top-level await; loading the Chapter One cast then auto-started a second harness on the live Canvas and resized it to 640×360. The same slow run showed `font-display: block` hiding otherwise-ready loading words. Explicit entry identity, chunk isolation, graph checks, and visible fallback type prevent all three production-only failures from returning |
| D92 | 2026-07-19 | **The brick-wall reveal keeps the courtyard until the wall itself opens** (refines D62): Diagon Alley is rendered at its final stable world scale from frame zero but clipped to the ten-by-eight wall bounds beneath the opaque brick cells. Moving bricks expose that destination only through their growing gaps; once the wall is visibly open, the same aperture expands across the courtyard and outgoing cast before the street becomes the active room with no second wipe | Painting the destination across the full canvas beneath only a wall-sized brick cover immediately replaced the sky, ground, and courtyard when the player tapped, leaving an intact wall floating over the next room. Confining the hidden destination to the physical wall preserves cause and effect: the bricks open first, reveal the street behind them, and then carry the player into it |
| D93 | 2026-07-19 | **Spoken words are recorded assets only, never browser text-to-speech**: every dialogue line, objective, recap, preview, letter, learning unit, and temporary placeholder that should speak must have a committed generated or family-recorded clip. Missing, failed, and interrupted playback keeps captions available and may settle with a nonverbal chime, but runtime code never calls a browser or operating-system voice. Architecture lint bans both Web Speech API entry points | A stale recorded clip could reject when a newer line interrupted it, which sent the old words to a robotic platform voice on top of the intended recording. Removing the fallback entirely and making the prohibition build-enforced prevents both that race and future placeholder shortcuts from changing Violet's cast |

## Open — needs Pete

| # | Question | What it blocks |
|---|----------|----------------|
| O3 | **Family cameos**: real pet's name as a preset, Theo in a portrait, dad as a shopkeeper, mom…? | Ch. 1 content detail pass |
| O5 | **Her reading specifics** (optional, sharpens the learning layer): does she know letter *sounds* vs letter *names*? Any sight words she already owns? Counting range comfortable to 10? | Tuning `vocabulary.js` and the Gentle/Stretchy dial defaults — can start with safe defaults and tune from playtests |
